
services:
    onedev-server:
        container_name: onedev-server
        restart: always
        volumes:
            - /pp:/var/run/docker.sock
            - /sss:/opt/onedev
        ports:
            - 6610:6610
            - 6611:6611
        image: 1dev/server
